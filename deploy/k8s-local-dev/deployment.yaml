apiVersion: apps/v1
kind: Deployment
metadata:
  name: kinetic-v3-api
  namespace: kinetic-v3
  labels:
    app: kinetic-v3-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kinetic-v3-api
  template:
    metadata:
      labels:
        app: kinetic-v3-api
    spec:
      containers:
        - name: api
          image: kinetic-v3-api:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: kinetic-v3-config
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
